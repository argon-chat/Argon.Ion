service FriendsInteraction() {

}


msg FriendRequest {
    requestId: guid;
    fromUser: guid;
    toUser: guid;
    requestTime: datetime;
}

enum FriendRequestStatus {
    Pending = 0,
    Accepted = 1,
    Declined = 2,
    Cancelled = 3,
    Expired = 4,
    Blocked = 5
}
